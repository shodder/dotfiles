# load git aliases and commands
if [ -f "$HOME/dotfiles/git/git-commands" ] ; then
    source "$HOME/dotfiles/git/git-commands"
fi

# load git aliases and commands
if [ -f "$HOME/dotfiles/git/git-releases" ] ; then
    source "$HOME/dotfiles/git/git-releases"
fi

export EDITOR=vim
export GIT_EDITOR=vim
export GREP_OPTIONS='--color=auto' GREP_COLOR='1;32'
export CLICOLOR=1

# Java related bits
#export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-amd64
#export MAVEN_HOME=/opt/maven
#export M2_HOME=/opt/maven
#PATH=${MAVEN_HOME}/bin:${PATH}


export WINHOME=/mnt/c/Users/mhr39349
export WS=$WINHOME/workspace
export BUSAPPS=$WINHOME/workspace/isisbusapps
export SITES=$WS/isisbusapps/SISites
export USERS=$WS/isisbusapps/Users
export USEROFFICETOOLS=$WS/isisbusapps/UserOfficeTools
export MESSAGES=$WS/isisbusapps/Messages
export STAFFDETAILS=$WS/isisbusapps/StaffDetails
export FBS=/mnt/c/fbs/Apps
export DOTS=~/dotfiles

alias dots='cd $DOTS'
alias winhome='cd $WINHOME'
alias ws='cd $WS'
alias busapps='cd $BUSAPPS'
alias ba='cd $BUSAPPS'
alias si='cd $SITES'
alias users='cd $USERS'
alias userofficetools='cd $USEROFFICETOOLS'
alias messages='cd $MESSAGES'
alias staffdetails='cd $STAFFDETAILS'
alias fbs='cd $FBS'


# kube
export KUBECONFIG_DEV_BLUE="$HOME/.kubecfg/kubernetes-kubeconfig-dev-blue"
export KUBECONFIG_DEV_GREEN="$HOME/.kubecfg/kubernetes-kubeconfig-dev-green"
export KUBECONFIG_PROD_BLUE="$HOME/.kubecfg/kubernetes-kubeconfig-prod-blue"
export KUBECONFIG_PROD_GREEN="$HOME/.kubecfg/kubernetes-kubeconfig-prod-green"

# DEFAULT - CONNECT TO DEV
export KUBECONFIG=$KUBECONFIG_DEV_BLUE

# Just checking
echo "Loaded shodders work profile"
echo "Using kubeconf: $KUBECONFIG \n\n"

